import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─────────────────────────────────────────
  // DARK MODE PALETTE — "OPS CENTER"
  // ─────────────────────────────────────────
  static const Color darkBgPrimary      = Color(0xFF0A0C10);
  static const Color darkBgSecondary    = Color(0xFF111318);
  static const Color darkBgTertiary     = Color(0xFF1A1E27);
  static const Color darkBgSurface      = Color(0xFF1E2330);
  static const Color darkBgOverlay      = Color(0xFF242838);
  static const Color darkBorderDefault  = Color(0xFF2A3040);
  static const Color darkBorderSubtle   = Color(0xFF1E2535);
  static const Color darkBorderFocus    = Color(0xFF00D4FF);
  static const Color darkTextPrimary    = Color(0xFFEAEDF3);
  static const Color darkTextSecondary  = Color(0xFF8892A4);
  static const Color darkTextMuted      = Color(0xFF4A5568);
  static const Color darkAccentBlue     = Color(0xFF00D4FF);
  static const Color darkAccentBlueDim  = Color(0x1A00D4FF);

  // ─────────────────────────────────────────
  // LIGHT MODE PALETTE — "HIGH-TECH CORPORATE"
  // ─────────────────────────────────────────
  static const Color lightBgPrimary     = Color(0xFFF8FAFC);
  static const Color lightBgSecondary   = Color(0xFFFFFFFF);
  static const Color lightBgTertiary    = Color(0xFFF1F5F9);
  static const Color lightBgSurface     = Color(0xFFFFFFFF);
  static const Color lightBgOverlay     = Color(0xFFF8FAFC);
  static const Color lightBorderDefault = Color(0xFFE2E8F0);
  static const Color lightBorderSubtle  = Color(0xFFF1F5F9);
  static const Color lightBorderFocus   = Color(0xFF0099EE);
  static const Color lightTextPrimary   = Color(0xFF0F172A);
  static const Color lightTextSecondary = Color(0xFF334155);
  static const Color lightTextMuted     = Color(0xFF64748B);
  static const Color lightAccentBlue    = Color(0xFF0099EE);
  static const Color lightAccentBlueDim = Color(0x1A0099EE);

  // ─────────────────────────────────────────
  // SHARED ACCENTS — identical in both modes
  // These are BRAND colors, not theme colors.
  // ─────────────────────────────────────────
  static const Color accentAmber        = Color(0xFFF0A500);
  static const Color accentAmberDim     = Color(0x26F0A500);
  static const Color accentCrimson      = Color(0xFFFF2D55);
  static const Color accentCrimsonDim   = Color(0x26FF2D55);
  static const Color accentGreen        = Color(0xFF00E676);
  static const Color accentGreenDim     = Color(0x1A00E676);
  static const Color accentPurple       = Color(0xFFB388FF);
  static const Color accentPurpleDim    = Color(0x1AB388FF);
  static const Color textOnAmber        = Color(0xFF0A0C10);
}
